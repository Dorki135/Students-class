<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>
  <!-- SDK implicitly includes all .cs files in the project directory, so no explicit <Compile> items are needed -->
</Project>
